interface AppLoadingShellProps {
    status?: string;
    error?: boolean;
}
/** Branded bootstrap / report-window loading state (replaces static index.html shell). */
export declare function AppLoadingShell({ status, error, }: AppLoadingShellProps): import("preact").JSX.Element;
export {};
